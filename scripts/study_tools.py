import pandas as pd
from


def create_input_sets(df:pd.DataFrame):
    """
    Input:
        - Variables to create study
        - Numbers for each variable or range
    Output:
        - Valid Input for calculation (DataClass), Validity check will be done by programm
    """
    return ...